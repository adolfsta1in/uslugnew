-- ============================================================================
-- Миграция: добавляет колонку `signature` в таблицу certificates.
-- Нужна для нового редактируемого поля подписи (бывшая линия «___» слева внизу
-- бланка). Выполните один раз в Supabase → SQL Editor, если таблица уже создана
-- по старой версии schema.sql.
-- ============================================================================

alter table public.certificates
  add column if not exists signature text;
