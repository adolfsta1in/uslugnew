-- ============================================================================
-- Миграция: добавляет колонку `certificate_number` в таблицу certificates.
-- № свидетельства теперь вводится в «Дополнительных данных» редактора и
-- переносится в колонку реестра «№ свидетельства» (раньше туда попадала серия
-- готового бланка cert_number, что было неверно).
-- Выполните один раз в Supabase → SQL Editor, если таблица уже создана.
-- ============================================================================

alter table public.certificates
  add column if not exists certificate_number text;
